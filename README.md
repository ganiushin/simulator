# Olympic Robot v4.0

## 📖 Использование

0. **Запуск**
   - Откройте файл index.html в браузере Chrome или Yandex Browser

1. **Загрузите Python скрипт**
   - Перетащите файл `.py` в область загрузки или нажмите "Выбрать файл"
   - Скрипт должен содержать `def run_robot(bot):` с простым кодом (см. пример в `user_script.py`)

2. **Запустите симулятор**
   - Нажмите "🎮 Запустить симулятор"
   - Дождитесь загрузки Pyodide (может занять 10-30 секунд при первом запуске)
   - Нажмите "▶ Старт" для запуска робота
   - Используйте кнопки управления:
     - **▶ Старт** - запустить выполнение скрипта
     - **⏸ Пауза** - приостановить симуляцию
     - **🔄 Сброс** - вернуть робота в начальную позицию
     - **🔘 Кнопка робота (B)** - нажать кнопку робота (или клавиша B на клавиатуре)
     - **✖ Закрыть** - закрыть симулятор


### Формат скрипта

- **`def run_robot(bot):`** — точка входа
- **`yield bot.sleep(seconds)`** — задержка (используем `yield` для неблокирующего выполнения)

Пример:

```python
def run_robot(bot):
    bot.motors.move(30, 30)
    yield bot.sleep(1.0)  # Используем yield для задержки!
    bot.motors.stop()
```

### Важные замечания

- Функция `run_robot` - функция-генератор (использует `yield`)
- Везде, где нужна задержка, используйте `yield bot.sleep(seconds)`
